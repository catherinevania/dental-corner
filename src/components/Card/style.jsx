import styled, { css } from "styled-components";
import { Button } from "../Button";
import { colors } from "../Colors";

export const Card = styled.div`
  width: 245px;
  height: 275px;
  filter: drop-shadow(0px 5px 15px #acacac);
  border-radius: 15px;
  border: 0px;
`;
